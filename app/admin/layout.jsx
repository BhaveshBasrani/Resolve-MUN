import "../globals.css";

export const metadata = {
  title: "Admin Panel | Resolve MUN 2.0",
  description: "Master Admin Dashboard for Resolve MUN 2.0",
  robots: { index: false, follow: false },
};

export default function AdminLayout({ children }) {
  return children;
}
